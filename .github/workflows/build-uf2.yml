name: Build UF2
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Generate Buildfiles
        run: |
          mkdir build
          cd build
          cmake .. -G Ninja
      - name: Execute Ninja build    
        run:
          ninja
