name: Build UF2
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
        mkdir build
        cd build
        cmake .. -G Ninja
        ninja
